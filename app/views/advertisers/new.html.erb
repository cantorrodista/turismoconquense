<div class="form_toolbar">
	<ul>
		<li>
			<%= link_to 'Volver', advertisers_path, :class => "back"%> 
		</li>
	</ul>
</div>

<% form_for @advertiser, :url => advertisers_path,
           :html => { :multipart => true} do |f| %>
        <%= render :partial => 'form', :locals => {:advertiser => @advertiser, :f => f} %>
      <% end %>

