<div class="form_toolbar">
	<ul>
		<li>
			<%= link_to 'Volver', highlights_path, :class => "back"%> 
		</li>
	</ul>
</div>
<% form_for @highlight, :url => highlights_path,
           :html => { :multipart => true} do |f| %>
        <%= render :partial => 'form', :locals => {:highlight => @highlight, :f => f} %>
<% end %>

