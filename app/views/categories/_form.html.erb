<%= f.error_messages %>
<fieldset>
    <p>
      <%= f.label :name, t('activerecord.attributes.category.name') %>
      <%= f.text_field :name %>
    </p>
 	<p>
  		<%= f.check_box :visible, :class => 'checkbox' %>
          <%= f.label :visible, t('activerecord.attributes.category.visible') %>
    </p>



<%= f.submit t('app.category.create.link_to') %>
